# 安全策略

## 适用范围

这是面向个人与小型内部工作空间的自托管网关，默认只监听回环地址。它不是
经过外部安全审计的托管服务，不实现多租户隔离、SSO 或商业账单。

## 报告漏洞

请通过 GitHub 的 **Security → Report a vulnerability** 私下提交，不要开公开 issue。
报告请包含：影响版本、复现步骤、实际影响。

作为个人项目，无法承诺响应时限或赏金。会尽力在确认后修复并在 CHANGELOG 中说明。

## 已知的信任边界

理解这些边界有助于判断某个行为是不是漏洞：

| 边界 | 说明 |
| --- | --- |
| 管理员令牌 | 可读配置、建调用 Key、改运行设置。**不能**把服务暴露到公网——那需要进程带 `--allow-remote` 启动 |
| 调用 Key | 只能访问模型端点，拿不到配置与凭证 |
| 上游凭证 | AES-GCM 加密后存 SQLite，主密钥是同目录的 `.key`。拥有数据库**和**配套 `.key` 的人可以还原凭证 |
| `data/` 目录 | 整体是敏感资产，数据库不是整库加密 |
| `--allow-remote` / `--tls-*` | 启动期边界，后台无法解除 |
| `/healthz` | 唯一免鉴权端点，只返回 ok/version/uptime |
| `/metrics` | 默认关闭；开启后仍需管理员令牌 |
| `log_level=debug` | 会记录管理接口错误详情，可能回显刚提交的配置片段。排障后请调回 |

## 不属于漏洞

- 拿到管理员令牌后能改配置：这是令牌的设计用途
- 配置导出含供应商名称与 Base URL：导出不含凭证，但仍是内部拓扑信息，不应公开发布
- 本地预算不等于供应商余额：没有官方余额 API，这是已声明的限制
- 演示模式返回固定文本：它明确标注为本地沙箱，不是模型

详细的部署与运维注意事项见 [docs/SECURITY.md](docs/SECURITY.md)。
