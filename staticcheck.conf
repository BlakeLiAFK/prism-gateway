# 在 staticcheck 默认规则集之上额外关闭两条：
# ST1005 要求错误信息首字母小写，但本项目错误信息是中文，
#        其中 "Base URL"、"TLS" 等是专有名词，规则不适用。
# ST1013 要求用 http.StatusXxx 常量，既有代码统一用数字字面量，
#        保持一致比局部替换更易读。
checks = ["inherit", "-ST1005", "-ST1013"]
